# castari-proxy (npm package)

This package exposes the `queryCastari` wrapper for `@anthropic-ai/claude-agent-sdk`.

For full documentation, see the repository root README or https://github.com/castar-ventures/castari-proxy.
